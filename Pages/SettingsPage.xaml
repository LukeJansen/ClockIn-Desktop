<Page
    x:Class="ClockIn_Desktop.Pages.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ClockIn_Desktop.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="helloTextBlock" Grid.Row="0" Style="{StaticResource HeaderTextBlockStyle}" Margin="25">Hello Name</TextBlock>
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <Button VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="24" Click="Change_Button_Click" Margin="25">Change Password</Button>
            <Button VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="24" Click="Logout_Button_Click">Logout</Button>
        </StackPanel>
    </Grid>
</Page>
