<Page
    x:Class="ClockIn_Desktop.Pages.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ClockIn_Desktop.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Orientation="Vertical" KeyDown="StackPanel_KeyDown">
        <TextBlock HorizontalAlignment="Center" Style="{StaticResource HeaderTextBlockStyle}" Margin="0 10 0 10">Login</TextBlock>
        
        <StackPanel Orientation="Horizontal" Margin="0 10 0 10">
            <TextBlock Style="{StaticResource TitleTextBlockStyle}" Width="125">Email</TextBlock>
            <TextBox x:Name="emailTextBox" Width="400" IsSpellCheckEnabled="False"></TextBox>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="0 5 0 5">
            <TextBlock Style="{StaticResource TitleTextBlockStyle}" Width="125">Password</TextBlock>
            <PasswordBox x:Name="passwordBox" Width="400"></PasswordBox>
        </StackPanel>

        <Button Margin="0 5 0 0" HorizontalAlignment="Center" Click="LoginButton_Click">Login</Button>
    </StackPanel>
</Page>
